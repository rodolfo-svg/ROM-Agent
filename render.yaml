# ═══════════════════════════════════════════════════════════════
# ROM AGENT - CONFIGURAÇÃO RENDER.COM
# ═══════════════════════════════════════════════════════════════
# Serviço Web Principal - Interface Claude AI + API Bedrock
# Deploy automático via GitHub (main branch)
# ═══════════════════════════════════════════════════════════════

services:
  # ═══════════════════════════════════════════════════════════════
  # PRODUÇÃO - Branch main
  # ═══════════════════════════════════════════════════════════════
  - type: web
    name: rom-agent
    runtime: node
    plan: free
    branch: main

    # ═══ BUILD ═══
    buildCommand: |
      echo "🔧 Instalando dependências..."
      npm ci --only=production
      echo "✅ Build concluído"

    # ═══ START ═══
    startCommand: npm run web:enhanced

    # ═══ VARIÁVEIS DE AMBIENTE ═══
    envVars:
      # Node.js
      - key: NODE_ENV
        value: production
      - key: NODE_VERSION
        value: 25.2.1

      # Porta (Render usa 10000)
      - key: PORT
        value: 10000

      # ✅ FORÇAR USO DO DISCO PERSISTENTE /var/data
      - key: RENDER
        value: "true"
      - key: UPLOAD_FOLDER
        value: /var/data/upload
      - key: EXTRACTED_FOLDER
        value: /var/data/extracted
      - key: PROCESSED_FOLDER
        value: /var/data/processed

      # AWS Bedrock (obrigatório)
      - key: AWS_ACCESS_KEY_ID
        sync: false
      - key: AWS_SECRET_ACCESS_KEY
        sync: false
      - key: AWS_REGION
        value: us-west-2

      # Anthropic (alternativo)
      - key: ANTHROPIC_API_KEY
        sync: false

      # DataJud (consulta jurisprudência)
      - key: DATAJUD_API_KEY
        sync: false

      # Session Secret (auto-gerado)
      - key: SESSION_SECRET
        generateValue: true

      # PostgreSQL Database (Internal Database URL)
      - key: DATABASE_URL
        value: postgresql://rom_agent_user:faPSk0YSNlhyPfBYpri2RcK9XdRbaE8L@dpg-d5819bhr0fns73dmfsv0-a/rom_agent

      # Rate Limiter
      - key: RATE_LIMIT_PER_MINUTE
        value: 10
      - key: RATE_LIMIT_PER_HOUR
        value: 100

    # ═══ HEALTH CHECK ═══
    healthCheckPath: /api/info

    # ═══ RECURSOS ═══
    # Disco persistente (criado automaticamente no primeiro deploy)
    disk:
      mountPath: /var/data
      sizeGB: 1

    # ═══ AUTO-DEPLOY ═══
    autoDeploy: true

    # ═══ DOMÍNIOS ═══
    domains:
      - iarom.com.br
      - www.iarom.com.br

  # ═══════════════════════════════════════════════════════════════
  # STAGING - Branch staging
  # ═══════════════════════════════════════════════════════════════
  - type: web
    name: rom-agent-staging
    runtime: node
    plan: free
    branch: staging

    # ═══ BUILD ═══
    buildCommand: |
      echo "🔧 [STAGING] Instalando dependências..."
      npm ci --only=production
      echo "✅ [STAGING] Build concluído"

    # ═══ START ═══
    startCommand: npm run web:enhanced

    # ═══ VARIÁVEIS DE AMBIENTE ═══
    envVars:
      # Node.js
      - key: NODE_ENV
        value: staging
      - key: NODE_VERSION
        value: 25.2.1

      # Porta (Render usa 10000)
      - key: PORT
        value: 10000

      # ✅ FORÇAR USO DO DISCO PERSISTENTE /var/data
      - key: RENDER
        value: "true"
      - key: UPLOAD_FOLDER
        value: /var/data/upload
      - key: EXTRACTED_FOLDER
        value: /var/data/extracted
      - key: PROCESSED_FOLDER
        value: /var/data/processed

      # AWS Bedrock (obrigatório)
      - key: AWS_ACCESS_KEY_ID
        sync: false
      - key: AWS_SECRET_ACCESS_KEY
        sync: false
      - key: AWS_REGION
        value: us-west-2

      # Anthropic (alternativo)
      - key: ANTHROPIC_API_KEY
        sync: false

      # DataJud (consulta jurisprudência)
      - key: DATAJUD_API_KEY
        sync: false

      # Session Secret (auto-gerado)
      - key: SESSION_SECRET
        generateValue: true

      # PostgreSQL Database (Internal Database URL)
      - key: DATABASE_URL
        value: postgresql://rom_agent_user:faPSk0YSNlhyPfBYpri2RcK9XdRbaE8L@dpg-d5819bhr0fns73dmfsv0-a/rom_agent

      # Rate Limiter (mais permissivo para testes)
      - key: RATE_LIMIT_PER_MINUTE
        value: 20
      - key: RATE_LIMIT_PER_HOUR
        value: 200

    # ═══ HEALTH CHECK ═══
    healthCheckPath: /api/info

    # ═══ RECURSOS ═══
    # Disco persistente separado para staging
    disk:
      mountPath: /var/data
      sizeGB: 1

    # ═══ AUTO-DEPLOY ═══
    autoDeploy: true

  # ═══════════════════════════════════════════════════════════════
  # FRONTEND V4 (STAGING) - React Static Site
  # ═══════════════════════════════════════════════════════════════
  - type: web
    name: rom-frontend-v4-staging
    env: static
    branch: staging

    # ═══ BUILD ═══
    buildCommand: |
      echo "🔧 [FRONTEND] Instalando dependências..."
      cd frontend
      npm ci
      echo "🏗️ [FRONTEND] Building React app..."
      npm run build
      echo "✅ [FRONTEND] Build concluído"

    # ═══ STATIC FILES ═══
    staticPublishPath: ./frontend/dist

    # ═══ VARIÁVEIS DE AMBIENTE ═══
    envVars:
      - key: NODE_VERSION
        value: 20

      # URL do backend staging (será atualizada após deploy)
      - key: VITE_API_URL
        value: https://rom-agent-staging.onrender.com

    # ═══ HEADERS (Segurança) ═══
    headers:
      - path: /*
        name: X-Frame-Options
        value: SAMEORIGIN

      - path: /*
        name: X-Content-Type-Options
        value: nosniff

      - path: /*
        name: Referrer-Policy
        value: strict-origin-when-cross-origin

    # ═══ ROTAS (SPA) ═══
    routes:
      - type: rewrite
        source: /*
        destination: /index.html

    # ═══ AUTO-DEPLOY ═══
    autoDeploy: true

    # ═══ PREVIEW DE PULL REQUESTS ═══
    pullRequestPreviewsEnabled: true
